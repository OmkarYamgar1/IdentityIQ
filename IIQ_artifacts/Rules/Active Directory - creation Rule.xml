<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE Rule PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<Rule created="1758207553022" id="c0a8006a995d1ab481995d562dfe0019" language="beanshell" modified="1758208989984" name="Active Directory - creation Rule" type="IdentityCreation">
  <Description>Identity creation rules are used to set attributes on new Identity objects when they are created.  New identities may be created during the aggregation of application accounts, or optionally created after pass-through authentication.

One common operation is to change the name property of the identity when the default application name is complex (such as a directory DN).

Another common operation is to assign a set of initial capabilities based on the attributes pulled from the application account.</Description>
  <Signature returnType="void">
    <Inputs>
      <Argument name="log">
        <Description>
          The log object associated with the SailPointContext.
        </Description>
      </Argument>
      <Argument name="context">
        <Description>
          A sailpoint.api.SailPointContext object that can be used to query the database if necessary.
        </Description>
      </Argument>
      <Argument name="environment" type="Map">
        <Description>
          Arguments passed to the aggregation task.
        </Description>
      </Argument>
      <Argument name="application">
        <Description>
          Application being aggregated.
        </Description>
      </Argument>
      <Argument name="account" type="ResourceObject">
        <Description>
          The resource account for the identity being created.
        </Description>
      </Argument>
      <Argument name="identity" type="Identity">
        <Description>
          The identity that is being created.
        </Description>
      </Argument>
    </Inputs>
  </Signature>
  <Source>import sailpoint.object.Identity;
  import sailpoint.object.Capability;
  import sailpoint.object.ResourceObject;

  // change the name to a combination of firstname and lastname
  String firstname = account.getStringAttribute("firstname");
  String lastname = account.getStringAttribute("lastname");
  String name  = firstname + "." + lastname;
  identity.setName(name);

  String dept=account.getStringAttribute("Department");
String type=account.getStringAttribute("Employee_type");

if("Regular".equalsIgnoreCase(type)){

    identity.setAttribute("type", "Regular");
  } 

  /*if("HR".equalsIgnoreCase(dept)){

    identity.setAttribute("Department", "HR");
  } else if ("IT".equalsIgnoreCase(dept)) {
    identity.setAttribute("Department", "Developer");
  } else if ("Sales".equalsIgnoreCase(dept)) {
    identity.setAttribute("Department", "Sales");
  } else if ("Marketing".equalsIgnoreCase(dept)) {
    identity.setAttribute("Department", "Finance");
  }else
  {
    return null;
  }*/</Source>
</Rule>
